# Optional runtime dependencies for Claude Code document-skills/pptx QA.
# LibreOffice and Poppler are system tools and must be installed separately.
jsonschema>=4.0
PyYAML>=6.0
Pillow>=10.0
markitdown[pptx]>=0.1.0
